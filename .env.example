# 复制成 .env 再填。.env 已在 .gitignore 里，不要提交。

# ---------------------------------------------------------------
# 移动端（Expo）
#
# EXPO_PUBLIC_ 前缀的变量会被打进客户端包，所以这里只能放公开值。
# anon key 是设计上就要公开的——真正的访问控制在数据库的 RLS 策略上。
#
# ★ mock 模式：下面两个 EXPO_PUBLIC_SUPABASE_* 只要有任意一个为空
#   （包括根本不建 .env 文件），App 就跑纯 mock：
#     - 跳过登录守卫，扫码直接进首页，不需要任何账号
#     - 全部数据来自 apps/mobile/lib/api.ts 的内存 mock，不发网络请求
#     - 上传简历、分析、采纳建议、导出 PDF 这条闭环都能走完
#   想用 Expo Go 先把 16 个界面过一遍，就别填这两个。
#   两个都填上之后，登录守卫自动生效。
# ---------------------------------------------------------------
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
# Next.js API 的地址。真机调试时不能用 127.0.0.1，要填电脑在局域网里的 IP。
EXPO_PUBLIC_API_URL=http://127.0.0.1:3000

# ---------------------------------------------------------------
# 服务端（Next.js）—— 以下任何一个都绝不能进移动端包
# ---------------------------------------------------------------
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Claude。AI_PROVIDER=mock 时不需要 key，全链路也能跑通（CI 用这个）。
#
# 注意优先级：**运营后台里启用的那条接入优先于这两个变量**。后台没配任何
# 接入（或压根没配 SUPABASE_SERVICE_ROLE_KEY）时才按这里来。
AI_PROVIDER=mock
ANTHROPIC_API_KEY=

# ---------------------------------------------------------------
# 运营后台（/admin）
#
# ★ 出厂账号是 admin / abc123，由迁移直接插进 admin_users。这个口令写在
#   README 里，等于全世界都知道——**公网部署前必须先登进 /admin/password
#   改掉**。没改之前后台每一页顶部都会挂一条红色警告条。
#   后台还需要上面的 SUPABASE_SERVICE_ROLE_KEY：它要跨全体用户汇总，
#   按定义越过 RLS。没有它后台起不来。
#
# 下面两把密钥一次生成、贴进 .env 即可：
#   echo "ADMIN_SESSION_SECRET=$(openssl rand -base64 32)"
#   echo "SETTINGS_ENCRYPTION_KEY=$(openssl rand -base64 32)"
# ---------------------------------------------------------------

# 运营后台会话 cookie 的签名密钥。生成一次：openssl rand -base64 32
#
# 没有内置默认值，也不会有：一把写在仓库里的密钥等于公开的密钥，任何读过
# 源码的人都能自己签一张票据直接进后台。所以 NODE_ENV=production 时缺它
# 会直接抛错，宁可后台打不开，也不能开着一扇假门。
# 少于 32 个字符同样拒收（生产和开发都拒）：一把能被离线爆破猜到的 HMAC
# 密钥和没有密钥是一回事，只是看上去更像配好了。上面那条命令出来是 44 个
# 字符，别自己想一个短的填进去。
# 本地开发不填也能跑（进程内临时生成一把，重启即登出，并打一条 warn）。
# 换掉它等于把所有人踢下线，这是预期行为。
ADMIN_SESSION_SECRET=

# 运营后台里填的 AI 平台 API Key 用它加密后存进 ai_providers。
# 生成一次：openssl rand -base64 32
#
# 生成后就不要再改：这是静态加密的主密钥，换一把之后已经存进数据库的 key
# 全部永久解不开，只能在后台一条条重填。缺失时后台的 AI 接入页会直接报错，
# 不会有默认值也不会随机生成——那样每次重启都会作废一批密文。
SETTINGS_ENCRYPTION_KEY=

# 允许后台里的接口地址（base_url）指向内网、本机，或使用明文 http。
#
# 默认关闭。关闭时服务端拒绝把请求发往 127.0.0.1 / 10.x / 192.168.x /
# 169.254.169.254（云元数据）这类地址，也拒绝 http——因为服务端发这个请求时
# 会把解密后的明文 API Key 放进请求头，地址填哪儿它就送到哪儿。
#
# 什么时候需要打开：局域网里自建了推理服务（Ollama、vLLM 等），接入配置要指
# 向它。打开之前先确认这台服务器所在的网段里没有别的不该被后台碰到的东西。
ADMIN_ALLOW_INSECURE_BASE_URL=

# ---------------------------------------------------------------
# OAuth（本地栈默认关闭这三家，见 supabase/config.toml）
# 拿到各平台凭据后填这里，并把 config.toml 里对应的 enabled 改成 true
# ---------------------------------------------------------------
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=
SUPABASE_AUTH_EXTERNAL_FACEBOOK_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_FACEBOOK_SECRET=
SUPABASE_AUTH_EXTERNAL_LINKEDIN_OIDC_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_LINKEDIN_OIDC_SECRET=
SUPABASE_AUTH_EXTERNAL_APPLE_SECRET=
